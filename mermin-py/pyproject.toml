[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "mermin"
dynamic = ["version"]
description = "k-atic alignment analysis of fluorescence microscopy"
requires-python = ">=3.10"
license = { text = "MIT OR Apache-2.0" }
authors = [{ name = "Alejandro Soto Franco" }]
dependencies = [
    "numpy>=1.24",
    "polars>=0.20",
    "cellpose>=3.0",
    "scikit-image>=0.22",
    "scipy>=1.12",
    "matplotlib>=3.8",
    "tifffile>=2024.1",
    "bioio>=3.0",
    "bioio-ome-tiff>=1.0",
    "bioio-ome-zarr>=2.0",
    "bioio-tifffile>=1.0",
]

[project.optional-dependencies]
vendor-formats = ["bioio-czi>=2.0", "bioio-lif>=2.0", "bioio-nd2>=2.0"]

[tool.maturin]
python-source = "../python"
module-name = "mermin._native"
features = ["pyo3/extension-module"]
exclude = ["corpus/**", "docs/**", "numerical-tests/**", "mermin-tests/**"]

[tool.pytest.ini_options]
markers = ["corpus: requires the ASF-EX1 mermin-corpus volume mounted"]
